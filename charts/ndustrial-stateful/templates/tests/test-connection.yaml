apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ndustrial-stateful.fullname" . }}-test-connection"
  labels:
{{ include "ndustrial-stateful.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ndustrial-stateful.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
