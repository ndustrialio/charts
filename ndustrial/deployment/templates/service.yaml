apiVersion: v1
kind: Service
metadata:
  name: {{ include "ndustrial-deployment.fullname" . }}
  labels:
    {{- include "ndustrial-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ndustrial-deployment.selectorLabels" . | nindent 4 }}
